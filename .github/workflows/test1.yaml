name: Tracee Pipeline Scan
on: [pull_request]

jobs:
  Tracee-Scan:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Start Tracee profiling in background
      uses: josedonizetti/tracee-action@673145bb6cf346a7a968865ff76aac3f97e0cd1a

    - name: Your CI Pipeline Step
      run: for i in {1..2}; do sleep 2; ps efx; done

    - name: change a file
      run: echo "hello" > hello.txt

    - name: Stop and Check Tracee results and create a PR
      uses: josedonizetti/tracee-action@55bd95e7ac4aa70002f41c53a92548dee26cce24
      with:
        fail-on-diff: "true"
        create-pr: "true"
